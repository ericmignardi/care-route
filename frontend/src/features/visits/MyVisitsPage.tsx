import { PagePlaceholder } from "../../components/layout/PagePlaceholder";

export function MyVisitsPage() {
  return (
    <PagePlaceholder
      eyebrow="Caregiver"
      title="My day"
      description="Today's visits in order, with the next one prominent and its check-in sized for a gloved thumb."
      next="Phase 5 builds the mobile day list, check-in and check-out, the task checklist and visit notes."
    />
  );
}
