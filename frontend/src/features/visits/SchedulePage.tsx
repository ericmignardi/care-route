import { PagePlaceholder } from "../../components/layout/PagePlaceholder";

export function SchedulePage() {
  return (
    <PagePlaceholder
      eyebrow="Coordinator"
      title="Schedule board"
      description="Caregivers down the left, hours across the top, unassigned visits in a rail of their own."
      next="Phase 5 builds the day view, the date navigation and the assign dialog that shows why each ineligible caregiver cannot take the slot."
    />
  );
}
