import { PagePlaceholder } from "../../components/layout/PagePlaceholder";

export function ClientsPage() {
  return (
    <PagePlaceholder
      eyebrow="Coordinator"
      title="Clients"
      description="Everyone in care, with their care plan and their full visit history."
      next="Phase 5 builds the paginated searchable table, the create and edit modal, and the client detail view with the care plan editor."
    />
  );
}
