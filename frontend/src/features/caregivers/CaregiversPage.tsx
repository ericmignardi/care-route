import { PagePlaceholder } from "../../components/layout/PagePlaceholder";

export function CaregiversPage() {
  return (
    <PagePlaceholder
      eyebrow="Coordinator"
      title="Caregivers"
      description="The workforce, their qualifications and the hours they actually work."
      next="Phase 5 builds the caregiver list, the skills editor and the weekly availability editor."
    />
  );
}
