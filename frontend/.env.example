# Base URL of the CareRoute API, including the /api/v1 path.
# Local development can leave this unset; the axios client defaults to the value below.
# In production this points at the Container App, which is what makes every request
# cross-site and requires SameSite=None on the auth cookie.
VITE_API_URL=http://localhost:8080/api/v1
